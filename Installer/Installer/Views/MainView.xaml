<UserControl x:Class="Installer.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Installer.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="AUTO" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>

        <ContentPresenter
            Grid.ColumnSpan="2"
            Content="{Binding CurrentPage}" />

        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Margin="0,0,5,0"
            Grid.Row="1"
            Grid.Column="1"
            Orientation="Horizontal">
            <Button
                Width="50"
                Margin="0,0,5,0"
                Content="Back"
                Visibility="{Binding CurrentPage.CanBack, Converter={StaticResource BoolToVisible}}"
                Command="{Binding BackCommand}" />
            <Button
                Width="50"
                Margin="0,0,5,0"
                Content="Next"
                Visibility="{Binding CurrentPage.CanNext, Converter={StaticResource BoolToVisible}}"
                Command="{Binding NextCommand}" />
            <Button
                Width="50"
                Margin="0,0,5,0"
                Content="Finish"
                Visibility="{Binding CurrentPage.CanFinish, Converter={StaticResource BoolToVisible}}"
                Command="{Binding FinishCommand}" />
            <Button
                Width="50"
                Margin="0,0,5,0"
                Content="Cancel"
                Visibility="{Binding CurrentPage.CanCancel, Converter={StaticResource BoolToVisible}}"
                Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>